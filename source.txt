class TOOO_HOLAMUNDO;
	create
		inicio;
		prueba;
	feature
		inicio is
		/*local
		do
		
	end /*inicio

	feature prueba(a:INTEGER) is
		do
			println 'p', 'r', 'u', 'e', 'b', 'a', a;
	end

	feature prueba(b:DOUBLE, z:INTEGER) is
		do
			println 'p', 'r', 'u', 'e', 'b', 'a', '2';
	end
		
end /*EJEMPLO

main
    prueba(5); 
end /*main